# ─── 데이터베이스 설정 (Docker 컨테이너 통신용) ───
# docker-compose.yml의 서비스 이름이 'db'라면 이대로 유지하십시오.
DB_HOST=db
DB_PORT=5432
DB_NAME=mornigdock
DB_USER=postgres
# 보안을 위해 'password' 대신 더 복잡한 값을 권장하지만, 개발 초기라면 유지 가능합니다.
DB_PASSWORD=password

# ─── JWT 보안 설정 (백엔드 전용 일급 기밀) ───
# 프론트엔드와 공유하지 않고 오직 백엔드만 알고 있어야 합니다.
JWT_SECRET=changethistoaverylongrandomstringmornigdock1!

# ─── 서버 구동 설정 ───
PORT=8787
NODE_ENV=development

# ─── CORS 허용 정책 ───
# 프론트엔드 앱이 실행 중인 주소를 정확히 일치시켜야 'CORS 에러'가 발생하지 않습니다.
FRONTEND_URL=http://localhost:5173