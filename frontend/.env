# ─── 프론트엔드 → 백엔드 API URL ───
# 브라우저가 백엔드 서버에 접속하기 위해 반드시 필요한 '이정표'입니다.
# 삭제 시 프론트엔드에서 데이터를 불러올 수 없습니다.
VITE_API_URL=http://localhost:8787

# ─── 삭제된 위험 요소 (보안 조치) ───
# DB_PASSWORD=... (프론트엔드에는 필요 없음, 노출 시 위험)
# JWT_SECRET=... (백엔드 전용, 프론트엔드에 두면 토큰 위조 가능)