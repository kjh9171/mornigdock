# ✅ Docker Compose 환경변수 파일
# 이 파일을 .env 로 복사하고 값을 변경하세요
# cp .env.example .env

# ─── 데이터베이스 ───
DB_PASSWORD=mornigdock1!

# ─── JWT 시크릿 ───
# 반드시 강력한 랜덤 문자열로 변경 필요!
# 생성 방법: openssl rand -base64 32
JWT_SECRET=changethistoaverylongrandomstringmornigdock1!

# ─── CORS 허용 프론트엔드 URL ───
FRONTEND_URL=http://localhost:80

# ─── 프론트엔드 → 백엔드 API URL ───
# 브라우저에서 접근하는 주소이므로 localhost 사용
VITE_API_URL=http://localhost:8787
