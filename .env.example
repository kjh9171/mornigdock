# ──────────────────────────────────────────────────────────
# 🏛️ AGORA - 환경변수 설정 파일
# 이 파일을 .env 로 복사 후 값을 수정하세요: cp .env.example .env
# ──────────────────────────────────────────────────────────

# ── 데이터베이스 ──────────────────────────────────────────
DB_USER=agora
DB_PASSWORD=agora_secret_2024_CHANGE_ME
DB_NAME=agora_db

# ── JWT 보안 키 (반드시 변경!) ────────────────────────────
JWT_SECRET=your_super_secret_jwt_key_minimum_32_characters_CHANGE_ME
JWT_REFRESH_SECRET=your_refresh_secret_key_minimum_32_characters_CHANGE_ME
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# ── 관리자 계정 ───────────────────────────────────────────
ADMIN_EMAIL=admin@agora.com
ADMIN_PASSWORD=Admin@1234!

# ── OTP 마스터 코드 (개발/비상용) ─────────────────────────
MASTER_OTP_CODE=000000

# ── 뉴스 API (NewsAPI.org 무료 키 발급: https://newsapi.org) ──
NEWS_API_KEY=your_news_api_key_here

# ── CORS / URL 설정 ───────────────────────────────────────
NODE_ENV=production
FRONTEND_URL=http://localhost:80
ALLOWED_ORIGINS=http://localhost:80,http://localhost:5173

# Vite 빌드 시 사용 (프론트엔드가 API를 바라보는 주소)
VITE_API_URL=http://localhost:8787

# ── Cloudflare Tunnel (선택) ──────────────────────────────
# cloudflared tunnel token (https://one.dash.cloudflare.com 에서 발급)
CLOUDFLARE_TUNNEL_TOKEN=
