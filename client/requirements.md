## Packages
framer-motion | Smooth animations for page transitions and UI elements
lucide-react | Beautiful icons for the interface
date-fns | Formatting dates for news and comments
recharts | Charts for the admin dashboard stats
react-hook-form | Form handling for auth and creation
@hookform/resolvers | Zod resolvers for react-hook-form
zod | Schema validation
clsx | Conditional class names
tailwind-merge | Merging tailwind classes

## Notes
- Theme uses Slate/Gray text with Amber/Orange accents
- Silver Mode implemented via CSS class on body/root
- Authentication includes MFA step if required
- Backend API follows standard REST patterns defined in shared/routes
