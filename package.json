{
  "name": "mornigdock",
  "private": true,
  "engines": {
    "node": ">=22.16.0",
    "npm": ">=10.9.0"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build:frontend": "npm run build -w frontend",
    "build:backend": "npm run build -w backend",
    "build:all": "npm run build:frontend && npm run build:backend",
    "deploy": "(cd backend && npx wrangler deploy) && (cd frontend && npx wrangler pages deploy dist --project-name=mornigdock-ui)"
  },
  "devDependencies": {
    "wrangler": "^3.109.2"
  }
}
